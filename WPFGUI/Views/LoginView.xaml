<UserControl x:Class="WPFGUI.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFGUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel LastChildFill="True">
        <StackPanel Background="White">
            <TextBlock Name="loginHead" Text="Login VvSdH-Tool" HorizontalAlignment="Center" FontSize="20" Margin="10"/>
            <TextBlock Name="text" Text="{Binding Info}" HorizontalAlignment="Center" Margin="10"/>
            <TextBox Name="username" Text="{Binding Username}" Margin="10" MaxWidth="250" Height="25"/>
            <TextBox Name="password" Text="{Binding Password}" Margin="10" MaxWidth="250" Height="25"/>
            <Button Name="login" Content="Login" Width="100" Height="25" HorizontalAlignment="Center" Margin="10" Command="{Binding LandCommand}"/>
            <TextBlock Name="copyright" Text="©2020 Gruppe 9" HorizontalAlignment="Center" Margin="10"/>
        </StackPanel> 
    </DockPanel>
</UserControl>
